name: todo_app
description: A simple to-do app with Firebase Auth
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.30.0
  firebase_auth: ^4.17.0
  cloud_firestore: ^4.12.0
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true